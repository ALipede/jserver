[
  {
    "type": "array",
    "minItems": 100,
    "maxItems": 200,
    "items": {
      "type": "string",
    "faker": {
      "id": 1,
      "fake": "{{name.lastName}}, {{name.firstName}} {{name.suffix}}"
    }
    }
  }
]
